name: "basic"
description: "Basic response with summary"
schema:
  type: "object"
  properties:
    response:
      type: "string"
      description: "The main response text"
    summary:
      type: "string"
      description: "A brief summary of the response"
  required: ["response", "summary"]
